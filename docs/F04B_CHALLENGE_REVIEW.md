# F04b 문장 검토·인용·제출

## 구현과 경계
- feature/F04b-challenge-review, 별도 worktree. origin/develop에서 분기하고 선행 F04a까지 가져온 HEAD eca4372 위에서 작업한다. 선행 커밋은 develop 미통합이다.
- PUT /api/v1/challenge-runs/{id}/reviews: 전체 버퍼 저장, 동일 문장 ID 유지·누락 검토 삭제, CAS, 최대 20개 검토/문장당 6개 인용.
- POST /api/v1/challenge-runs/{id}/submit: 미검토 허용, 제출 시 버전 증가·시각 저장·읽기 전용, 재전송은 같은 제출 상태. 평가 요청/가짜 결과를 만들지 않는다.
- 서버는 소유권·ACTIVE/CHALLENGE·IN_PROGRESS·동일 초안 문장·공개 원자료·줄 범위를 검사한다. 실제 인용문을 서버에서 추출하고 USER/ACCEPTED로 저장한다.
- 사용자 보고서와 최초 제출본은 수정하지 않는다. 정답 키·오류 수를 브라우저에 보내지 않는다.
- UI: 유지/수정/근거 부족, 이유·수정문, 원자료 줄 번호·인용 미리보기, 근거 삭제, 명시적 저장·복원, 미검토 수 안내·제출 확인. 실패 입력 보존, 서버 재조회 시 미저장 폐기 확인.
- V10으로 도메인 테이블 11개. evidence_links는 수동 검산 연결만 지원한다. claims/XOR/AI 후보 연결·전체 22개 스키마·정식 seed는 미완료.
- 의존성·계약 변경 없음. 생성 타입은 기존 계약을 재사용한다.

## 검증
- F04a 실제 로그인→작성→제출→검산 열람·새로고침 연결은 2026-09-12 사용자가 완료 확인했다. F04b 수동 확인과 구분한다.
- 최초 검사에서 기존 테이블 수 기대값을 갱신했고, JSON 역직렬화와 의미 검증을 분리해 400/422 응답을 바로잡았다. 새 웹 테스트의 Testing Library 옵션 타입 오류도 수정했다. 최초 E2E는 선택 상자/알림 선택자가 맞지 않아 실패했다. 접근성 역할 및 Next 알림과 구분하는 선택자로 수정했다. 배경 재조회 실패 시 미저장 버퍼가 사라질 수 있는 UI 경로도 수정하고 회귀 검사를 추가했다.
- 실제 Google 로그인 기반 F04b 흐름은 사용자가 정상 수행했다고 확인했다. 문장별 판단·인용·저장·새로고침 복원·미검토 안내·제출 잠금·보고서 보존을 수동 확인했다. 두 실제 계정·장시간 토큰 갱신은 미실행. 자동 E2E는 로컬 서명 JWT와 실제 API/DB를 사용한다.

## 로컬 환경
- 테스트: web3169/API8269/DB55499, 별도 Compose 프로젝트 doezip-f04b. 이전 F04a DB55489와 작업 공간을 보존한다.
- 실제 Google 확인용 WEB_PORT3129 설정은 준비했지만 테스트와 일반 실행을 구분한다. F04a 실행기만 종료하고 F04b를 web3129/API8269에서 실행한다. 기존 데이터는 별도 DB에 보존하므로 새 과제를 시작한다.
- PR·merge·배포는 수행하지 않는다. 커밋/push 상태는 Git으로 확인한다.

## 최종 결과
- `npm run check` 성공: 웹 lint/typecheck/단위 66/build, API test/build 49, 실제 PostgreSQL Testcontainers, Playwright 32, 공개 fixture 21·OpenAPI·타입 재생성 일치. 실패·skip 없음.
- 검토 인용 저장/복원/제출 잠금, stale 입력 보존, 제출 응답 유실 재시도, 저장·제출 경합, 보고서 보존 검사 통과. 320px 제출 화면 캡처 확인.
- 변경 33개 파일의 실제 환경값/생성물 제외 검사, git diff --check 통과. 계약·lockfile 변경 없음.
- F04b 변경은 별도 기능 커밋으로 공유한다. 원격 CI 결과는 해당 커밋의 GitHub Actions에서 확인한다. PR·merge·배포 없음. F04a 원격 상태와 원본 F00 변경은 보존한다.

- 공유 준비에서는 사용자 수동 검증 기록을 갱신했다. 실행 코드는 최종 전체 검사 이후 변경하지 않았으며, 앞선 자동 검사 결과를 이번에 재실행한 것으로 표시하지 않는다.
