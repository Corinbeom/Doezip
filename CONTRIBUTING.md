# 기여 가이드

## 기능 단위 작업

기능 작성자가 화면·API·DB·테스트까지 맡고 상대방이 리뷰한다.
동일 기능의 프론트엔드와 백엔드는 같은 브랜치에서 작업한다.
Codex를 동시에 실행할 때는 별도 clone 또는 worktree를 사용한다.
에이전트 작업 규칙은 [AGENTS.md](AGENTS.md), 실행 명령은 [README.md](README.md)를 따른다.

## 브랜치와 병합

- `main`: 배포 기준. 직접 기능 개발 금지.
- `develop`: 통합 기준. 직접 기능 개발 금지.
- `feature/*`: 최신 `develop`에서 분기한다. 기능 PR 대상은 `develop`이다.
- 상대방 1명의 승인과 CI 통과 후 기능 PR을 squash merge한다. 병합한 기능 브랜치는 재사용하지 않는다.
- `develop` → `main`은 배포 검증 후 merge commit으로 반영하고, 결과를 `develop`에도 동기화한다.
- 공유 브랜치의 강제 push와 임의 히스토리 재작성은 금지한다.

## 커밋·푸시·리뷰

- 관련 파일만 stage하고 diff, 비밀값, 불필요한 생성물 포함 여부를 확인한다.
- 메시지에 작업 종류와 기능 ID를 적는다. 예: `chore(F00): 개발 환경 구성`.
- 실행 코드가 바뀌면 `npm run check`를 수행한다. 문서만 바뀌면 링크·명령·내용 일치와 diff를 확인한다.
- PR에 실행한 검사·미실행 항목과 이유를 구분해 기록한다. 이전 실행 결과를 새 검증으로 표시하지 않는다.
- 공통 UI·계약·의존성·DB 변경에는 영향 범위와 리뷰 대상을 적는다.
- 푸시는 브랜치를 공유하는 단계다. CI·리뷰·배포 검증을 통과한 것으로 간주하지 않는다.

## GitHub 설정 체크리스트 (미적용)

저장소 관리자가 적용할 항목이다. 체크리스트가 있다는 이유로 적용 완료로 간주하지 않는다.

- [ ] `main`/`develop` 보호 규칙 또는 ruleset 적용, 직접 push 제한.
- [ ] PR 승인 1명, 오래된 승인 무효화, 대화 해결 요구.
- [ ] 필수 상태 검사 `environment` 통과 요구.
- [ ] 강제 push와 브랜치 삭제 제한.
- [ ] 기능 PR은 squash merge, 배포 PR은 merge commit으로 운영하도록 저장소 옵션 확인.

## 구현 범위

후속 작업은 [기능 백로그](docs/FEATURE_BACKLOG.md), 디자인 승인 상태는
[디자인 자료](docs/design/README.md)에서 관리한다.
